# CloudPress SQLite Integration

Bundled WordPress plugin installed automatically during CloudPress WordPress creation.

It marks the WordPress runtime as using the CloudPress php-wasm SQLite shard layer and provides admin visibility for the integration state.
